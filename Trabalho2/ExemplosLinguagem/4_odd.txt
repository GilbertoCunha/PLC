int count, for;
int seq[5] = [1, 10, 7, 6, 3];

# Iterar a sequência
for (count, 0, 5) ::

    # Verificar se o número é ímpar
    if seq[count]%2 == 0 ::
        write("O elemento {count} do array com valor {seq[count]} é ímpar\n");
    :;
:;

write("O número de elementos ímpares é {count}\n");