int N = read()
int i, r, var

# Ler o primeiro número
r = read()

# Criar ciclo
for (i, 0, N-1) do
    var = read()
    
    # Verificar se o novo elemento é menor que o resultado
    if var < r then
        r = var

write("O menor número é {r}\n")